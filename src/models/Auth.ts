export type GetNonceType = {
  address: string;
};

export type GetNonceData = {
  address: string;
  nonce: string;
};

export type GetTokenType = {
  address: string;
  signature: string;
};

export type GetTokenData = {
  accessToken: string;
};
