import { ProfileUpdate } from 'views/Profile';
import { PublicLayout } from 'layouts';

const Home = () => {
  return (
    <PublicLayout>
      <ProfileUpdate />
    </PublicLayout>
  );
};

export default Home;
